# ============================================================
# Telepsicología Segura - Variables de Entorno
# ============================================================
# SEGURIDAD: Copiar este archivo como .env.local y rellenar
# los valores reales. NUNCA subir .env.local a Git.
# ============================================================

# --- Firebase Admin SDK ---
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
# SEGURIDAD: La private key debe estar en formato PEM completo
# entre comillas dobles: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# --- Google Cloud KMS (Principio 5: Cifrado AES-256/KMS) ---
GCP_KMS_KEY_NAME=projects/YOUR_PROJECT/locations/YOUR_LOCATION/keyRings/YOUR_RING/cryptoKeys/YOUR_KEY
# Formato requerido: projects/{project}/locations/{location}/keyRings/{ring}/cryptoKeys/{key}

# --- Anthropic API (Claude) ---
ANTHROPIC_API_KEY=
# Formato: sk-ant-api03-...

# --- Aplicación ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# --- Firebase Emuladores (desarrollo local) ---
FIRESTORE_EMULATOR_HOST=localhost:8080
FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
